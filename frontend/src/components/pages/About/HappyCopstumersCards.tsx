interface CostumersCardProps {
    image: string;
    name: string;
    description: string;
}

export default function HappyCostumersCards(props: CostumersCardProps) {
    return(
        <div>
            
        </div>
    )
}